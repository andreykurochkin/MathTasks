@using MathTasks.Extensions
@model Guid
@if (ViewData["DoHideLink"] is not null)
{
    <span>Hidden</span>
}
else
{
    <a asp-controller="AlterMathTasks" asp-action="Show" asp-route-id="@Model" asp-route-returnUrl="@Context.GetReturnUrl()">View</a>
}
