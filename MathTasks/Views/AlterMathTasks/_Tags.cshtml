@model IEnumerable<MathTasks.ViewModels.TagViewModel>

@foreach (var tag in Model)
{
    <a asp-controller="AlterMathTasks" asp-action="Index" asp-route-tag="@tag.Name" class="badge bg-light text-dark text-decoration-none">@tag.Name</a>
}